/**
 * Props de autoria do design `COUTS Landing.dc.html`.
 *
 * No canvas do Claude Design elas eram controles ajustáveis; aqui viram
 * constantes com os mesmos valores padrão, para ficarem fáceis de achar.
 */

/** `heroDim` — escurecimento sobre o vídeo do hero, em % (0–70 no design). */
export const HERO_DIM_PERCENT = 15;

/** `videoSpeed` — velocidade de reprodução do vídeo do hero (0.25–2). */
export const VIDEO_SPEED = 1;
